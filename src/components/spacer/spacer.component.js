import React from 'react'

const SpacerComponent = ({ space = 3 }) => {
    return (
        <div style={{ marginTop: `${space*8}px` }}>
            
        </div>
    )
}

export default SpacerComponent
